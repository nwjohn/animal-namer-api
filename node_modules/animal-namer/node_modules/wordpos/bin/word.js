#!/usr/bin/env node

var program = require('commander'),
  _ = require('underscore')._,
  fs = require('fs'),
  WordPos = require('../src/wordpos'),
  wordpos = new WordPos(),
  util = require('util'),
  results = {},
  cmd = null;

var word = process.argv.slice(2)[0];
console.log(word);

wordpos.getPOS([word], console.log);